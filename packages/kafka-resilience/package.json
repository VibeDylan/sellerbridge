{
  "name": "kafka-resilience",
  "version": "0.0.1",
  "description": "Retry-with-headers + dead letter topic helper for kafkajs consumers",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "kafkajs": "^2.2.4"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
