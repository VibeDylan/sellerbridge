services:
  mongo:
    image: mongo:7.0
    container_name: mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    env_file:
      - path: .env
        required: true
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
